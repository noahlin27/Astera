<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.noahlin.blog4u.dao.UserDAO">
    <resultMap id="UserResult" type="top.noahlin.blog4u.model.User">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="headUrl" column="head_url"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <sql id="TABLE_NAME">
        user
    </sql>
    <sql id="INSERT_FIELDS">
        name, password, salt, head_url, is_deleted
    </sql>
    <sql id="SELECT_FIELDS">
        id, name, password, salt, head_url, is_deleted
    </sql>

<!--    <insert id="insertUser" parameterType="top.noahlin.blog4u.model.User">-->
<!--        insert into demo_user(name, password, salt, headUrl, isDeleted)-->
<!--        values (#{ame}, #{password}, #{salt}, #{head_url}, #{is_deleted})-->
<!--    </insert>-->

    <select id="selectById" resultMap="UserResult">
        select
        <include refid="SELECT_FIELDS"></include>
        from
        <include refid="TABLE_NAME"></include>
    </select>

<!--    <update id="updUser" parameterType="top.noahlin.blog4u.model.User">-->
<!--        update demo_user-->
<!--        set name=#{name}, password=#{password},  salt=#{salt}, headUrl=#{head_url}, isDeleted#{is_deleted}-->
<!--        where id=#{id}-->
<!--    </update>-->

<!--    <delete id="delUser" parameterType="int">-->
<!--        delete from demo_user where id=#{id}-->
<!--    </delete>-->
</mapper>